import React from "react";
import { Button } from "react-bootstrap";

const ModeSelect = () => {
    return (
        <div>
        <Button>
            Addition
        </Button>

        <Button>
            Subtraction   
        </Button>
        </div>
    );
};

export default ModeSelect;